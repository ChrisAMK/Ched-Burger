<h1>Cats!</h1>

<h2>Burgers ready to Eat!</h2>

<ul>
   {{#each burgers}}
        {{#unless devoured}}
            <tr>
            <td>{{this.id}}</td>
            <td>{{this.burger_name}}</td>
            <td><button type="button" class="alert button devour-it" data-id="{{this.id}}">Devour It!</button></td>
            </tr>
        {{/unless}}
    {{/each}}
</ul>

<h2>Burgers that were too bloody scrumptious! and got eaten :0</h2>

<ul>
    {{#each burgers}}
        {{#if devoured}}
            <tr>
            <td>{{this.id}}</td>
            <td>{{this.burger_name}}</td>
            <td><button type="button" class="alert button delete" data-id="{{this.id}}">Delete</button></td>
            </tr>
        {{/if}}
    {{/each}}
</ul>

<h2>Add a Burger!</h2>
<form class="create-form">

  <div class="form-group">
    <label for="ca">Burger Name:</label>
    <input type="text" id="burger_name" name="name">
  </div>

  <div class="form-group">
    <label for="devoured">Devoured</label><br>
    <input type="radio" name="devoured" value="1" checked> Devoured!<br>
    <input type="radio" name="devoured" value="0"> Ready to be Devoured!
  </div>

  <button type="submit">Add Burger</button>
</form>
